import React from 'react';
import Submain from '../../components/home/Submain';
// import { useSelector } from 'react-redux';

const SubmainContainer = () => {
  return <Submain />;
};

export default SubmainContainer;
